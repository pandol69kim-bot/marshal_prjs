# 나의 한계

## 지식/정보
- **학습 데이터 컷오프: 2025년 8월** — 이후 출시된 라이브러리, 취약점, 이벤트는 모름
- 실시간 인터넷 검색 불가 (WebSearch 툴 호출 시는 가능)
- 내부 시스템, 사내 문서, 비공개 코드베이스 접근 불가

## 코드/실행
- **현재 디렉토리(`D:\Marchall_CLAUDE\m_prj1\output`) 밖 파일 접근 제한** — 권한 설정에 따라 다름
- GUI 조작 불가 (브라우저 클릭, 화면 직접 제어 등)
- 장시간 실행 프로세스 모니터링 한계 — 백그라운드 작업 결과를 실시간으로 보지 못함
- Windows 전용 명령어 일부 오작동 가능 (Unix 셸 기준으로 동작)

## 판단/추론
- **확신처럼 보이지만 틀릴 수 있음** — 특히 에러 원인 추측, 복잡한 런타임 동작
- 코드를 "읽어서" 동작을 추론하지만, 실제 실행 결과와 다를 수 있음
- 대규모 코드베이스 전체를 한 번에 파악하는 데 한계 (컨텍스트 창 제한)

## 이 프로젝트에서 구체적으로
- Docker 컨테이너 **내부** 로그/상태를 실시간으로 볼 수 없음 (직접 `docker logs` 실행 필요)
- OAuth2 실제 작동 여부는 Google/Kakao 실제 키 없이 검증 불가
- Maven 빌드 중간 오류는 실행해봐야 알 수 있음

---

**결론:** 코드 읽기, 분석, 수정, 계획 수립은 잘 하지만 — **실행하고 결과 보는 건 직접 해야** 합니다. 의심스러운 부분은 항상 실행으로 검증해주세요.
