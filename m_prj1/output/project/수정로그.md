# 수정 로그

---

## [2026-04-16] docker-compose.dev.yml 오류 수정

### 메타 정보
- **날짜:** 2026-04-16
- **작업명:** Docker Compose dev 환경 서비스 의존성 오류 수정
- **대상 파일:** `docker-compose.dev.yml`

### 프롬프트 (사용자 요청)
```
오류체크 \project>docker compose -f docker-compose.yml -f docker-compose.dev.yml up
service "frontend" depends on undefined service "backend": invalid compose project
```

### 오류 원인
`docker-compose.dev.yml`에서 `backend` 서비스에 `profiles: [disabled]`를 설정하면,
Docker Compose가 해당 서비스를 **"미정의 서비스(undefined service)"** 로 취급한다.

`frontend` 서비스의 `depends_on: - backend`가 미정의 서비스를 참조하게 되어
프로젝트 로드 시점에 유효성 검사 오류가 발생한다.

### 수정 내용

**수정 전 (`docker-compose.dev.yml`)**
```yaml
services:
  backend:
    profiles:
      - disabled

  frontend:
    ...
    depends_on: []
```

**수정 후 (`docker-compose.dev.yml`)**
```yaml
services:
  backend:
    image: alpine
    command: sh -c "echo 'Backend is running locally on host (not in Docker)' && tail -f /dev/null"
    ports: []
    environment: {}
    depends_on: {}
    healthcheck:
      test: ["CMD", "true"]
      interval: 30s
      timeout: 5s
      retries: 1
    restart: "no"

  frontend:
    ...
    depends_on:
      - backend
```

### 변경 사항 상세

| 항목 | 변경 전 | 변경 후 |
|------|---------|---------|
| `backend` 비활성화 방식 | `profiles: [disabled]` | `alpine` 이미지 no-op 컨테이너 |
| `backend` 포트 | 기본값 유지 | `[]` (포트 없음) |
| `backend` 환경변수 | 기본값 유지 | `{}` (없음) |
| `backend` 재시작 정책 | `unless-stopped` | `"no"` |
| `frontend.depends_on` | `[]` | `- backend` |

### 결과
- 더미 `backend` 컨테이너가 먼저 시작 (8080 포트 없음 → 로컬 실행 백엔드와 충돌 없음)
- `frontend` Vite dev server가 이후 정상 시작
- `docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d` 오류 해결

### 특이사항
- `profiles: [disabled]`는 Docker Compose 공식 "비활성화" 기능이 아님
  - 해당 프로필이 활성화되지 않으면 서비스가 "미정의"로 처리되어 의존성 오류 발생
- no-op `alpine` 컨테이너는 로컬에서 직접 실행되는 실제 백엔드(`localhost:8080`)와 독립적으로 동작

---

## [2026-04-16] Vite `@` 별칭 미해석 오류 수정

### 메타 정보
- **날짜:** 2026-04-16
- **작업명:** Docker 컨테이너 내 Vite 설정 파일 누락으로 인한 모듈 해석 실패 수정
- **대상 파일:** `docker-compose.dev.yml`

### 프롬프트 (사용자 요청)
```
오류체크
frontend-1  | Error: The following dependencies are imported but could not be resolved:
frontend-1  |   @/components/ui/BaseToast.vue (imported by /app/src/App.vue?id=0)
frontend-1  |   @/stores/auth.store (imported by /app/src/router/index.ts)
frontend-1  |   @/pages/LoginPage.vue (imported by /app/src/router/index.ts)
frontend-1  |   ... (이하 동일)
```

### 오류 원인
`Dockerfile.dev`는 `package*.json`만 컨테이너에 복사하고,
`docker-compose.dev.yml` 볼륨 마운트에 **`vite.config.ts`가 포함되지 않았다.**

Vite는 `@` → `/app/src` 별칭 매핑을 `vite.config.ts`의 `resolve.alias`에서 읽는다.
컨테이너 안에 이 파일이 없으므로 `@/...` 경로를 해석하지 못해 모든 컴포넌트를 찾을 수 없게 된다.

```
# 컨테이너 내부 실제 상태 (수정 전)
/app/
├── package.json       ← COPY됨
├── node_modules/      ← npm install됨
├── src/               ← volume mount됨
│   └── ...
│
# 없음: vite.config.ts, tsconfig*.json
```

### 수정 내용

**수정 전 (`docker-compose.dev.yml` volumes)**
```yaml
volumes:
  - ./frontend/src:/app/src
  - ./frontend/public:/app/public
  - ./frontend/index.html:/app/index.html
  - /app/node_modules
```

**수정 후 (`docker-compose.dev.yml` volumes)**
```yaml
volumes:
  - ./frontend/src:/app/src
  - ./frontend/public:/app/public
  - ./frontend/index.html:/app/index.html
  - ./frontend/vite.config.ts:/app/vite.config.ts        # @ 별칭 해석에 필수
  - ./frontend/tsconfig.json:/app/tsconfig.json
  - ./frontend/tsconfig.app.json:/app/tsconfig.app.json
  - ./frontend/tsconfig.node.json:/app/tsconfig.node.json
  - /app/node_modules
```

### 변경 사항 상세

| 추가된 마운트 | 용도 |
|-------------|------|
| `vite.config.ts` | `@` 별칭 정의 (`resolve.alias`) — **핵심** |
| `tsconfig.json` | TypeScript 기본 설정 |
| `tsconfig.app.json` | 앱 소스 TypeScript 설정 |
| `tsconfig.node.json` | Vite 설정 파일용 TypeScript 설정 |

### 결과
- `@/components/...`, `@/stores/...`, `@/pages/...` 등 모든 `@` 경로 정상 해석
- Vite dev server 정상 기동

### 특이사항
- 볼륨 마운트 방식으로 추가했으므로 로컬에서 `vite.config.ts` 수정 시 컨테이너 재빌드 없이 반영됨
- 컨테이너 재시작이 필요한 경우: `docker compose ... restart frontend`

---

## [2026-04-16] Tailwind CSS 미적용 오류 수정

### 메타 정보
- **날짜:** 2026-04-16
- **작업명:** Docker 컨테이너 내 Tailwind/PostCSS 설정 파일 누락으로 인한 CSS 미적용 수정
- **대상 파일:** `docker-compose.dev.yml`

### 프롬프트 (사용자 요청)
```
http://localhost:5173/login  css 적용이 안되고 있는데 확인해줘.
```

### 오류 원인
`docker-compose.dev.yml` 볼륨 마운트에 **`tailwind.config.js`와 `postcss.config.js`가 없었다.**

Vite는 CSS 처리 시 PostCSS를 통해 `@tailwind base/components/utilities` 디렉티브를 실제 CSS로 변환한다.
이 변환은 `postcss.config.js`가 있어야 작동하며, `tailwind.config.js`가 없으면 `content` 경로를 찾지 못해 클래스를 생성하지 못한다.

```
# CSS 처리 흐름
main.css (@tailwind 디렉티브)
  → PostCSS (postcss.config.js 필요)
    → Tailwind CSS 플러그인 (tailwind.config.js 필요)
      → 실제 CSS 클래스 생성
```

컨테이너에 두 파일이 없으면 `@tailwind` 디렉티브가 빈 CSS로 처리되어 스타일이 전혀 적용되지 않는다.

### 수정 내용

**수정 전 (`docker-compose.dev.yml` volumes)**
```yaml
volumes:
  - ./frontend/src:/app/src
  - ./frontend/vite.config.ts:/app/vite.config.ts
  - ./frontend/tsconfig.json:/app/tsconfig.json
  ...
```

**수정 후 (`docker-compose.dev.yml` volumes)**
```yaml
volumes:
  - ./frontend/src:/app/src
  - ./frontend/vite.config.ts:/app/vite.config.ts
  - ./frontend/tailwind.config.js:/app/tailwind.config.js  # 추가
  - ./frontend/postcss.config.js:/app/postcss.config.js    # 추가
  - ./frontend/tsconfig.json:/app/tsconfig.json
  ...
```

### 변경 사항 상세

| 추가된 마운트 | 용도 |
|-------------|------|
| `tailwind.config.js` | content 경로, theme(primary 색상 등) 정의 — Tailwind 클래스 생성에 필수 |
| `postcss.config.js` | `@tailwind` 디렉티브를 실제 CSS로 변환하는 PostCSS 파이프라인 설정 |

### 결과
- `@tailwind base/components/utilities` 정상 처리
- `bg-gray-50`, `text-gray-900`, `rounded-xl`, `card` 등 모든 Tailwind 클래스 적용
- `/login` 페이지 스타일 정상 렌더링

### 적용 방법
```bash
docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build frontend -d
```

---

## [2026-04-16] Spring Boot 기동 실패 — `jwt.*` 속성 미해석 수정

### 메타 정보
- **날짜:** 2026-04-16
- **작업명:** `application.yml` 누락으로 인한 Spring Boot 속성 플레이스홀더 해석 실패 수정
- **대상 파일:** `backend/src/main/resources/application.yml` (신규 생성)

### 프롬프트 (사용자 요청)
```
Caused by: java.lang.IllegalArgumentException:
  Could not resolve placeholder 'jwt.access-token-expiry' in value "${jwt.access-token-expiry}"
```

### 오류 원인
`application.yml`이 git에서 삭제된 상태였다 (커밋 `bd171c1`).

Spring Boot 기동 흐름:
1. 기본 `application.yml` 로드 → **파일 없음**
2. `application-local.yml` 로드 → 프로필이 활성화되지 않으면 로드 안 됨
3. `JwtProvider`가 `${jwt.access-token-expiry}` 주입 시도 → 속성 없음 → **IllegalArgumentException**

`jwt.*` 속성은 `application-local.yml`에만 정의되어 있었으나,
`local` 프로필이 활성화되지 않으면 해당 파일 자체가 로드되지 않는다.

### 수정 내용

**신규 생성: `application.yml`**
- 모든 `@Value` 속성을 환경변수 참조로 정의
- 비밀이 아닌 값에는 `:default` 기본값 제공
- `JWT_SECRET`처럼 필수 시크릿은 기본값 없이 환경변수 강제

```yaml
jwt:
  secret: ${JWT_SECRET}                          # 환경변수 필수
  access-token-expiry: ${JWT_ACCESS_TOKEN_EXPIRY:900}
  refresh-token-expiry: ${JWT_REFRESH_TOKEN_EXPIRY:604800}
```

### application.yml vs application-local.yml 역할 분리

| 파일 | 역할 |
|------|------|
| `application.yml` | 모든 환경 공통 기본값. 시크릿은 환경변수 참조 |
| `application-local.yml` | 로컬 개발용 더미값 오버라이드 (git 커밋 가능) |

### 로컬 실행 방법

`application-local.yml`이 로드되려면 **반드시 `local` 프로필을 활성화**해야 한다:

```bash
# Windows
./mvnw.cmd spring-boot:run -Dspring-boot.run.profiles=local

# 또는 환경변수로
set SPRING_PROFILES_ACTIVE=local
./mvnw.cmd spring-boot:run
```

### 결과
- Spring Boot 기동 시 `jwt.*`, `s3.*`, `app.*` 등 모든 속성 정상 해석
- `local` 프로필 활성화 시 `application-local.yml`이 환경변수 없이도 동작
- 프로덕션 환경에서는 환경변수로 모든 값 주입

---

## [2026-04-16] 어드민 계정 추가

### 메타 정보
- **날짜:** 2026-04-16
- **작업명:** 초기 어드민 로그인 계정 추가 (Flyway 마이그레이션)
- **대상 파일:** `backend/src/main/resources/db/migration/V3__add_admin_user.sql` (신규 생성)

### 프롬프트 (사용자 요청)
```
어드민 로그인 계정 하나 추가해줘
```

### 추가된 계정 정보

| 항목 | 값 |
|------|----|
| 이메일 | `admin@example.com` |
| 비밀번호 | `Admin1234!` |
| 이름 | 관리자 |
| 역할 | `ADMIN` |
| 로그인 방식 | `LOCAL` |

### 수정 내용

**신규 생성: `V3__add_admin_user.sql`**
```sql
INSERT INTO users (email, password, name, role, provider)
VALUES (
    'admin@example.com',
    crypt('Admin1234!', gen_salt('bf', 10)),
    '관리자',
    'ADMIN',
    'LOCAL'
)
ON CONFLICT (email) DO NOTHING;
```

### 기술 결정 사항
- `pgcrypto`의 `crypt('bf', 10)` 사용 → Spring Security `BCryptPasswordEncoder`(`$2a$10$...`) 와 완전 호환
  - V1 마이그레이션에서 이미 `CREATE EXTENSION IF NOT EXISTS "pgcrypto"` 설치됨
- `ON CONFLICT (email) DO NOTHING` 추가 → 멱등성 보장 (재실행 시 중복 오류 없음)

### 적용 방법
Spring Boot 재기동 시 Flyway가 V3를 자동 실행:
```bash
./mvnw.cmd spring-boot:run -Dspring-boot.run.profiles=local
```

---

## [2026-04-16] Docker backend-1 기동 실패 — `jwt.*` 속성 미해석 (이미지 캐시 문제)

### 메타 정보
- **날짜:** 2026-04-16
- **작업명:** Docker 백엔드 이미지 재빌드 필요 (application.yml 미포함)
- **대상 파일:** 코드 수정 없음 — 재빌드 명령 안내

### 프롬프트 (사용자 요청)
```
backend-1 | Caused by: java.lang.IllegalArgumentException:
  Could not resolve placeholder 'jwt.access-token-expiry' in value "${jwt.access-token-expiry}"
```

### 오류 원인
이전 세션에서 `application.yml`을 신규 생성했으나,
Docker 백엔드 이미지는 **그 이전에 빌드된 캐시된 이미지**를 사용 중이었다.

```
Dockerfile 빌드 흐름:
  COPY src ./src          ← application.yml 포함
  RUN ./mvnw package      ← jar 생성
  → jar 안에 application.yml 없음 (이전 빌드 캐시 사용)
```

`--build` 없이 `docker compose up`을 실행하면 기존 이미지를 재사용하므로
새로 추가된 `application.yml`이 jar에 포함되지 않아 속성 해석 실패.

### 해결 방법
코드 변경 없음 — 이미지 재빌드만 필요:

```bash
docker compose -f docker-compose.yml up --build backend -d
```

### 특이사항
- `--build` 플래그는 소스 변경(Java 코드, 설정 파일) 후 항상 필요
- `src/main/resources/` 파일 변경 시에도 반드시 재빌드해야 jar에 반영됨
- `.env` 파일의 `JWT_SECRET=your-secret-key-minimum-32-characters-long`은 32자 이상으로 유효함

---

## [2026-04-16] 이메일 로그인 정상 작동 여부 체크 및 수정

### 메타 정보
- **날짜:** 2026-04-16
- **작업명:** 이메일 로그인 버그 2건 수정
- **대상 파일:**
  - `frontend/src/lib/axios.ts`
  - `frontend/src/router/index.ts`
  - `frontend/src/pages/LoginPage.vue`

### 발견된 버그

---

#### 버그 1 (치명적) — Axios 인터셉터 데드락으로 로그인 무한 로딩

**파일:** `frontend/src/lib/axios.ts`

**증상:** 이메일/비밀번호를 입력하고 로그인 버튼을 눌러도 로딩 스피너가 무한히 돌고 에러가 표시되지 않음

**원인:**

```
로그인 POST /api/v1/auth/login → 401 반환
  └─ 인터셉터: isRefreshing=false → isRefreshing=true, 갱신 시도
       └─ 갱신 POST /api/v1/auth/refresh → 401 반환
            └─ 인터셉터: isRefreshing=true → failedQueue에 추가 (대기)
                 ← 갱신 완료를 기다림 (갱신이 큐에서 대기 중) → 데드락
```

`/auth/` 엔드포인트에서 받은 401은 "토큰 만료"가 아닌 "인증 실패"이므로
토큰 갱신을 시도해서는 안 된다.

**수정 내용:**

```diff
+ // 인증 엔드포인트는 즉시 에러 반환 (갱신 시도 불필요)
+ if (originalRequest.url?.includes('/auth/')) {
+   return Promise.reject(error)
+ }
  if (error.response?.status === 401 && !originalRequest._retry) {
```

---

#### 버그 2 — `/register` 라우트 미등록으로 회원가입 페이지 접근 불가

**파일:** `frontend/src/router/index.ts`, `frontend/src/pages/LoginPage.vue`

**증상:** `RegisterPage.vue`가 존재하지만 라우터에 등록되지 않아 `/register` 접근 시 404, 로그인 페이지에 회원가입 링크 없음

**수정 내용:**

`router/index.ts`:
```diff
+ {
+   path: '/register',
+   name: 'register',
+   component: () => import('@/pages/RegisterPage.vue'),
+   meta: { requiresGuest: true }
+ },
```

`LoginPage.vue`:
```diff
+ <p class="mt-4 text-center text-sm text-gray-600">
+   계정이 없으신가요?
+   <RouterLink to="/register" ...>회원가입</RouterLink>
+ </p>
```

### 정상 로그인 흐름 확인

| 단계 | 내용 | 상태 |
|------|------|------|
| 폼 유효성 검사 | zod 스키마로 이메일/비밀번호 형식 검사 | ✅ |
| API 요청 | `POST /api/v1/auth/login` | ✅ |
| 백엔드 응답 | `ApiResponse<LoginResponse>` 형식 일치 | ✅ |
| 에러 표시 | `error.detail` 파싱 | ✅ (버그 1 수정 후) |
| 토큰 저장 | `localStorage.accessToken` | ✅ |
| 갱신 쿠키 | `refreshToken` HttpOnly 쿠키 | ✅ |
| 리다이렉트 | 로그인 후 `/dashboard` 이동 | ✅ |

---

## [2026-04-16] nginx `/login` 프록시 충돌로 Vue 로그인 페이지 미렌더링 수정

### 메타 정보
- **날짜:** 2026-04-16
- **작업명:** nginx `location /login` 이 Vue Router `/login` 페이지를 Spring Boot로 프록시하는 충돌 수정
- **대상 파일:** `frontend/nginx.conf`

### 프롬프트 (사용자 요청)
```
frontend-1 | GET /login?redirect=/dashboard HTTP/1.1  302
frontend-1 | GET /login HTTP/1.1  200
frontend-1 | GET /oauth2/authorization/google HTTP/1.1  302
```

### 오류 원인

nginx `location /login` 이 `/login`으로 시작하는 **모든** 경로를 백엔드로 프록시:

```
브라우저 GET /login?redirect=/dashboard
  → nginx: location /login 매칭 → Spring Boot로 프록시
  → Spring Security: /login 처리 → 302 redirect (쿼리스트링 제거)
  → 브라우저 GET /login
  → nginx: 다시 Spring Boot로 프록시
  → Spring Security 기본 로그인 폼 반환(200)
  → Vue 로그인 페이지 대신 Spring Security 화면 렌더링
```

nginx의 `/login` prefix 매칭은 `/login/oauth2/code/google` (OAuth2 콜백)을 프록시하려는 의도였으나,
Vue Router의 `/login` 경로도 함께 캡처하여 Spring Boot로 전달됨.

### 수정 내용

**수정 전 (`nginx.conf`)**
```nginx
location /login {
    proxy_pass http://backend:8080;
    ...
}
```

**수정 후 (`nginx.conf`)**
```nginx
# /login/oauth2/** 만 프록시 (OAuth2 콜백 전용)
location /login/oauth2 {
    proxy_pass http://backend:8080;
    ...
}
```

### nginx 라우팅 결과 (수정 후)

| 경로 | nginx 처리 | 결과 |
|------|-----------|------|
| `/login` | `location /` → `try_files` | Vue SPA `index.html` 반환 ✅ |
| `/login?redirect=/dashboard` | `location /` → `try_files` | Vue SPA, 쿼리스트링 유지 ✅ |
| `/login/oauth2/code/google` | `location /login/oauth2` → 백엔드 | Spring Security OAuth2 콜백 ✅ |
| `/oauth2/authorization/google` | `location /oauth2` → 백엔드 | Google 인증 시작 ✅ |

### 적용 방법
```bash
docker compose -f docker-compose.yml up --build frontend -d
```

---

## [2026-04-16] OAuth2 기능 테스트 — 버그 3건 수정

### 메타 정보
- **날짜:** 2026-04-16
- **작업명:** OAuth2 로그인 (Google/Kakao) 버그 분석 및 수정
- **대상 파일:**
  - `backend/.../handler/OAuth2SuccessHandler.java`
  - `backend/.../service/OAuth2UserService.java`

---

### 버그 1 (치명적) — Kakao 로그인 시 `email = null` → 사용자 조회 실패

**파일:** `OAuth2SuccessHandler.java`

**원인:**
```java
// 수정 전 — Google만 동작, Kakao는 null 반환
String email = oAuth2User.getAttribute("email");
User user = userRepository.findByEmail(email); // null → 예외
```

Kakao 사용자 정보 구조:
```json
{
  "id": 12345,
  "kakao_account": {
    "email": "user@kakao.com",   ← 여기에 있음
    "profile": { "nickname": "홍길동" }
  }
}
```
Google은 `email`이 최상위 속성이지만, Kakao는 `kakao_account.email`에 중첩되어 있음.

**수정:**
```java
// registrationId로 Provider 판별 후 email 추출
private String extractEmail(String registrationId, OAuth2User oAuth2User) {
    if ("kakao".equals(registrationId)) {
        Map<String, Object> kakaoAccount = oAuth2User.getAttribute("kakao_account");
        if (kakaoAccount == null) return null;
        return (String) kakaoAccount.get("email");
    }
    return oAuth2User.getAttribute("email"); // Google 등 OIDC
}
```

---

### 버그 2 (높음) — OAuth2 로그인 후 Refresh Token 미발급

**파일:** `OAuth2SuccessHandler.java`

**원인:**
OAuth2 성공 핸들러가 access token만 URL 파라미터로 전달하고, refresh token HttpOnly 쿠키를 설정하지 않음.
→ 토큰 만료(15분) 후 자동 갱신 불가 → 강제 로그아웃

**수정:**
```java
// access token + refresh token 모두 발급
String accessToken  = jwtProvider.generateAccessToken(...);
String refreshToken = jwtProvider.generateRefreshToken(userId);

// 이메일 로그인과 동일하게 HttpOnly 쿠키 설정
Cookie cookie = new Cookie("refreshToken", refreshToken);
cookie.setHttpOnly(true);
cookie.setPath("/api/v1/auth");
cookie.setMaxAge((int) jwtProvider.getRefreshTokenExpiry());
response.addCookie(cookie);
```

---

### 버그 3 (중간) — Kakao 이메일 미동의 시 `null` email로 DB 저장 시도

**파일:** `OAuth2UserService.java`

**원인:**
카카오 계정의 이메일 제공에 동의하지 않은 경우 `email = null` 상태로
`User.createOAuth2(null, name, provider, providerId)` 호출 →
`users.email NOT NULL` 제약조건 위반 → SQL 예외 (에러 메시지 불명확)

**수정:**
```java
if (email == null) {
    throw new OAuth2AuthenticationException(
        "이메일 제공에 동의하지 않아 로그인할 수 없습니다. 이메일 동의 후 다시 시도해주세요.");
}
```

---

### OAuth2 전체 플로우 (수정 후)

```
[브라우저] /oauth2/authorization/google
  → [nginx] → [Spring Security] → Google 로그인 페이지

[Google 인증 완료]
  → [nginx /login/oauth2/code/google] → [Spring Security 콜백]
  → OAuth2UserService.loadUser() → 사용자 조회/생성
  → OAuth2SuccessHandler.onAuthenticationSuccess()
    ├── Provider별 email 추출 (Google: attribute, Kakao: kakao_account.email)
    ├── access token 발급
    ├── refresh token 발급 + HttpOnly 쿠키 설정  ← 추가
    └── redirect: /oauth2/callback?token=...

[Vue OAuth2CallbackPage]
  → token 저장 → fetchMe() → /dashboard 이동
```

### 적용 방법
```bash
docker compose -f docker-compose.yml up --build backend -d
```

---

## [2026-04-16] Google OAuth2 500 오류 — `/oauth2/callback` nginx 프록시 충돌 수정

### 메타 정보
- **날짜:** 2026-04-16
- **작업명:** nginx `location /oauth2` 가 Vue Router `/oauth2/callback` 페이지를 Spring Boot로 프록시하는 충돌 수정
- **대상 파일:** `frontend/nginx.conf`

### 프롬프트 (사용자 요청)
```json
{
  "success": false,
  "error": {
    "type": "https://api.example.com/errors/internal",
    "title": "Internal Server Error",
    "status": 500,
    "detail": "예기치 않은 오류가 발생했습니다",
    "instance": "/oauth2/callback"
  }
}
```

### 오류 원인

`OAuth2SuccessHandler`가 인증 완료 후 `http://localhost/oauth2/callback?token=...` 으로 리다이렉트.
nginx의 `location /oauth2` prefix 매칭이 이 경로를 Spring Boot로 프록시:

```
[Google 인증 완료]
  → OAuth2SuccessHandler redirect → /oauth2/callback?token=...
  → nginx: location /oauth2 매칭 → Spring Boot로 프록시
  → Spring Boot: GET /oauth2/callback 핸들러 없음
  → GlobalExceptionHandler.handleUnexpected() → 500 응답
```

`location /oauth2`는 `/oauth2/authorization/{provider}` (인증 시작) 만 프록시하려는 의도였으나,
`/oauth2/callback` (Vue Router 페이지) 도 함께 캡처하여 Spring Boot로 전달됨.

### 수정 내용

**수정 전 (`nginx.conf`)**
```nginx
location /oauth2 {
    proxy_pass http://backend:8080;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    ...
}
```

**수정 후 (`nginx.conf`)**
```nginx
# /oauth2/authorization/{provider} 만 프록시 (/oauth2/callback 은 Vue 라우트)
location /oauth2/authorization {
    proxy_pass http://backend:8080;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}
```

### nginx 라우팅 결과 (수정 후)

| 경로 | nginx 처리 | 결과 |
|------|-----------|------|
| `/oauth2/authorization/google` | `location /oauth2/authorization` → 백엔드 | Google 인증 시작 ✅ |
| `/oauth2/authorization/kakao` | `location /oauth2/authorization` → 백엔드 | Kakao 인증 시작 ✅ |
| `/oauth2/callback?token=...` | `location /` → `try_files` | Vue SPA `OAuth2CallbackPage.vue` 반환 ✅ |
| `/login/oauth2/code/google` | `location /login/oauth2` → 백엔드 | Spring Security OAuth2 콜백 ✅ |

### nginx 전체 라우팅 정리 (최종)

```nginx
location /api             → 백엔드 (REST API)
location /oauth2/authorization → 백엔드 (OAuth2 인증 시작)
location /login/oauth2    → 백엔드 (OAuth2 콜백 처리)
location /oauth2/callback → location / → Vue SPA (토큰 수신 페이지)
location /login           → location / → Vue SPA (로그인 페이지)
location /                → try_files → Vue SPA (모든 Vue 라우트)
```

### 적용 방법
```bash
docker compose -f docker-compose.yml up --build frontend -d
```

---

## [2026-04-16] UI 반응형 리디자인 — 사이드바, 햄버거, 컬러 시스템

### 메타 정보
- **날짜:** 2026-04-16
- **작업명:** 반응형 레이아웃 + 현대적 미니멀 디자인 시스템 적용
- **대상 파일:**
  - `frontend/tailwind.config.js`
  - `frontend/src/assets/main.css`
  - `frontend/src/components/layouts/AppLayout.vue`
  - `frontend/src/components/layouts/AuthLayout.vue`
  - `frontend/src/pages/LoginPage.vue`

---

### 1. 컬러 팔레트 교체 (`tailwind.config.js`)

기존 파란색(blue) 계열에서 인디고(indigo) 계열로 전환.  
모든 `primary-*` 클래스가 자동으로 새 색상으로 반영됨.

| 항목 | 변경 전 | 변경 후 |
|------|---------|---------|
| primary-600 | `#2563eb` (blue) | `#4f46e5` (indigo) |
| primary-500 | `#3b82f6` | `#6366f1` |
| 전체 느낌 | 기본 파란색 | 현대적 인디고/바이올렛 계열 |

---

### 2. 글로벌 스타일 정제 (`main.css`)

- `bg-gray-*` → `bg-zinc-*` (쿨톤, 더 정제된 중성색)
- 카드 스타일: `shadow-sm` 제거 → border만 사용 (플랫 미니멀)
- 폰트 렌더링: `-webkit-font-smoothing: antialiased` 추가
- input-field, btn-primary/secondary 전반 zinc 색상 계열로 통일

---

### 3. AppLayout 반응형 사이드바 (`AppLayout.vue`)

#### 데스크톱 (lg 이상)
- 고정 사이드바 w-60 (240px), `lg:translate-x-0` 항상 표시
- 메인 콘텐츠 `lg:pl-60`으로 사이드바 공간 확보

#### 모바일/태블릿 (lg 미만)
- 상단 topbar (h-16) — 햄버거 버튼 + 로고
- 햄버거 클릭 시 사이드바 슬라이드 인 (`translate-x-0`)
- 바깥 영역 클릭 시 사이드바 닫힘 (`-translate-x-full`)
- 반투명 backdrop overlay (`bg-black/30 backdrop-blur-sm`)
- Vue `<Transition name="fade">` 로 overlay 부드럽게 전환

#### 사이드바 구성 요소

| 영역 | 내용 |
|------|------|
| 로고 | 번개 아이콘 + "API Service" 텍스트 |
| 네비게이션 | 아이콘 + 텍스트 (대시보드 / 리소스 / 알림관리) |
| 활성 메뉴 | `bg-primary-50 text-primary-700` |
| 사용자 영역 | 이니셜 아바타 + 이름/이메일 + 로그아웃 버튼 |

#### 인라인 SVG 아이콘
외부 라이브러리 없이 인라인 SVG 사용 (번들 크기 최소화):
- 대시보드: 집 아이콘 / 리소스: 레이어 아이콘 / 알림: 벨 아이콘 / 로그아웃: 화살표 아이콘

---

### 4. AuthLayout 미니멀 배경 (`AuthLayout.vue`)

```
기존: gradient-to-br from-primary-50 to-primary-100 (진한 그라데이션)
변경: zinc-50 + 대형 blur 원형 장식 2개 (subtle 배경)
```

blur 처리된 반투명 원형 배경으로 깊이감 연출:
- 우상단: `bg-primary-100/40 blur-3xl`
- 좌하단: `bg-primary-50/60 blur-3xl`

---

### 5. 로그인 페이지 (`LoginPage.vue`)

- 카드: `rounded-xl` → `rounded-2xl` (더 부드러운 모서리)
- 상단: 로고 아이콘 + 타이틀 + 서브타이틀
- 구분선: `h-px bg-zinc-100` (더 세밀한 선)
- 카카오 버튼: 공식 브랜드 컬러 `#FEE500` 정확히 적용
- 소셜 아이콘: Google SVG, Kakao SVG 통일

---

### 적용 방법
dev 환경은 Vite HMR로 자동 반영. 프로덕션 빌드 시:
```bash
docker compose -f docker-compose.yml up --build frontend -d
```

---

## [2026-04-16] 알림 관리 기능 버그 3건 수정

### 메타 정보
- **날짜:** 2026-04-16
- **작업명:** 알림 관리 페이지 정상 작동 체크 및 버그 수정
- **대상 파일:**
  - `frontend/src/router/index.ts`
  - `frontend/src/api/notifications.api.ts`
  - `frontend/src/pages/admin/NotificationPage.vue`

---

### 버그 1 (치명적) — 라우터 가드 순서 오류로 어드민 페이지 접근 불가

**파일:** `frontend/src/router/index.ts`

**증상:** 어드민 계정으로 로그인 후 `/admin/notifications`를 새로고침하면 `/dashboard`로 튕김

**원인:**
```
기존 guards 실행 순서:
  1. requiresAuth → 통과
  2. requiresGuest → 해당 없음
  3. requiresAdmin: authStore.isAdmin 체크  ← user가 null이므로 false → /dashboard로 리다이렉트!
  4. fetchMe() ← 도달하지 못함
```

새로고침 시 `accessToken`은 localStorage에 있으나 `user`는 메모리에 없음.
`isAdmin`은 `user?.role === 'ADMIN'`이므로 user가 null이면 항상 `false`.
`fetchMe()` 전에 `requiresAdmin` 체크가 실행되어 어드민도 dashboard로 리다이렉트됨.

**수정:**
```diff
- if (to.meta.requiresAdmin && !authStore.isAdmin) {
-   return { path: '/dashboard' }
- }
  if (authStore.isAuthenticated && !authStore.user) {
    try {
      await authStore.fetchMe()  // ← 먼저 실행
    } catch { ... }
  }
+ if (to.meta.requiresAdmin && !authStore.isAdmin) {
+   return { path: '/dashboard' }
+ }
```

---

### 버그 2 (높음) — SMS 채널 선택 시 `phone` 필드 누락

**파일:** `frontend/src/api/notifications.api.ts`, `NotificationPage.vue`

**증상:** SMS를 선택해도 전화번호 입력 필드가 없어 발송 불가

**원인:**
백엔드 `SendNotificationRequest`에는 `phone` 필드가 있으나 프론트 타입/폼에서 누락됨:
```java
// 백엔드 SendNotificationRequest.java
public record SendNotificationRequest(
    String userId, String channel,
    String phone,   // ← 있음
    String title, String content
) {}
```

**수정:**
```typescript
// notifications.api.ts
export interface SendNotificationRequest {
  userId: string
  channel: 'EMAIL' | 'SMS'
  phone?: string   // SMS 채널 선택 시 필수 (추가)
  title: string
  content: string
}
```

폼에서 `v-if="sendForm.channel === 'SMS'"` 조건부 렌더링으로 phone 입력 필드 추가.

---

### 버그 3 (중간) — 발송 후 이력 목록 미갱신

**파일:** `NotificationPage.vue`

**증상:** 알림 발송 성공 후 발송 이력 테이블이 즉시 갱신되지 않음

**원인:**
백엔드가 `@Async`로 비동기 발송 처리 → 컨트롤러는 즉시 `success: true` 반환.
프론트에서 발송 성공 후 `invalidateQueries` 호출이 없어 캐시된 이전 목록이 유지됨.

**수정:**
```typescript
// 비동기 발송 후 1.5초 딜레이를 두고 목록 갱신 (DB 기록 시간 확보)
setTimeout(() => {
  queryClient.invalidateQueries({ queryKey: ['notifications'] })
}, 1500)
```

---

### 추가 개선 사항

**UX 개선 (`NotificationPage.vue`):**
- 빈 이력 상태 메시지 추가 (기존: 빈 테이블 / 변경: 안내 문구 + 아이콘)
- 이력 테이블 수동 새로고침 버튼 추가
- 채널 배지 색상 구분 (EMAIL: 파란색, SMS: 보라색)
- 상태 한글 표시 (SENT → 발송됨, FAILED → 실패, PENDING → 대기중)
- 발송 중 로딩 스피너 + 비활성화 처리
- 스타일 zinc 계열 통일

### 정상 동작 흐름 (수정 후)

```
1. 어드민 로그인 → /admin/notifications 접근
2. 라우터 가드: fetchMe() 선 실행 → isAdmin = true → 통과
3. 채널 EMAIL 선택: userId + title + content 입력 → 발송
4. 채널 SMS 선택: userId + phone + title + content 입력 → 발송
5. 발송 성공 → 1.5초 후 이력 목록 자동 갱신
6. 발송 이력: PENDING → SENT/FAILED 상태 확인
```

---

## [2026-04-16] 알림 발송 — 이메일 입력 시 UUID 파싱 오류 수정

### 메타 정보
- **날짜:** 2026-04-16
- **작업명:** `NotificationService` 사용자 조회 로직 개선 (이메일/UUID 양방향 지원)
- **대상 파일:**
  - `backend/.../notification/service/NotificationService.java`
  - `frontend/src/pages/admin/NotificationPage.vue`

### 오류 내용
```
java.lang.IllegalArgumentException: Invalid UUID string: pandol69.kim@gmail.com
  at NotificationService.java:28 — UUID.fromString(userId)
```

### 오류 원인

사용자가 "사용자 ID" 폼 필드에 이메일 주소를 입력했으나,
백엔드 `NotificationService`가 `UUID.fromString(userId)` 로 파싱을 시도하여 예외 발생.

```java
// 수정 전 (오류 발생)
User user = userRepository.findById(UUID.fromString(userId)).orElse(null);
//                                   ↑ 이메일 문자열이 들어오면 즉시 IllegalArgumentException
```

### 수정 내용

**`NotificationService.java` — `findUser()` 헬퍼 추가**

`sendEmail()`, `sendSms()` 양쪽의 `UUID.fromString(userId)` 를 `findUser(userId)` 호출로 교체.

```java
private User findUser(String userIdOrEmail) {
    if (userIdOrEmail != null && userIdOrEmail.contains("@")) {
        return userRepository.findByEmail(userIdOrEmail).orElse(null);
    }
    try {
        return userRepository.findById(UUID.fromString(userIdOrEmail)).orElse(null);
    } catch (IllegalArgumentException e) {
        log.warn("잘못된 사용자 식별자 형식: {}", userIdOrEmail);
        return null;
    }
}
```

**`NotificationPage.vue` — 라벨/플레이스홀더 안내 개선**

```diff
- 사용자 ID (UUID)  /  placeholder="xxxxxxxx-xxxx-..."
+ 사용자 (이메일 또는 UUID)  /  placeholder="user@example.com 또는 UUID"
```

### 동작 흐름 (수정 후)

| 입력값 | 조회 방식 | 결과 |
|--------|-----------|------|
| `user@example.com` | `findByEmail()` | 이메일로 사용자 조회 ✅ |
| `550e8400-e29b-41d4-...` | `findById(UUID)` | UUID로 사용자 조회 ✅ |
| `invalid-string` | UUID 파싱 실패 → warn 로그 | null 반환, 발송 건너뜀 ✅ |

### 적용 방법
```bash
docker compose -f docker-compose.yml up --build backend -d
```

---

## [2026-04-16] 알림 관리 수정 미반영 — 프론트엔드 이미지 재빌드 누락 수정

### 메타 정보
- **날짜:** 2026-04-16
- **작업명:** 알림 관리 프론트/백엔드 변경사항 Docker 컨테이너 반영 및 실동작 검증
- **원인:** 소스 수정 후 프론트엔드 이미지 재빌드 누락

### 문제 상황

이전 세션에서 알림 관리 기능 버그 3건(라우터 가드, phone 필드, 목록 갱신)과
UUID 파싱 오류를 수정했으나 "수정 반영 안된듯" 증상 지속.

**원인 진단:**
```
프론트엔드 변경 파일:
  - router/index.ts          ← 라우터 가드 순서 수정
  - notifications.api.ts     ← phone 타입 추가
  - NotificationPage.vue     ← phone 필드, 목록 갱신, UX 개선

→ 소스 파일만 수정됨
→ 프론트엔드 Docker 이미지(project-frontend)는 이전 빌드 그대로
→ nginx가 구버전 번들 파일 서빙 중 → 변경사항 미반영
```

### 조치 내용

프론트엔드 + 백엔드 동시 이미지 재빌드 및 재기동:
```bash
docker compose -f docker-compose.yml up --build frontend -d
```

빌드 결과 (프론트엔드):
```
✓ 179 modules transformed
NotificationPage-DBB4jnPy.js   6.39 kB  ← 수정된 페이지 번들
AppLayout-CBIO3ByF.js          6.41 kB  ← 반응형 레이아웃 포함
✓ built in 7.84s
```

### API 동작 검증 결과

어드민 로그인 후 이메일로 알림 발송 테스트:

```
POST /api/v1/notifications/send
{ "userId": "admin@example.com", "channel": "EMAIL", "title": "test", "content": "test" }
→ { "success": true }  ✅

GET /api/v1/notifications?page=0&size=5
→ total: 1, channel=EMAIL, status=FAILED, title=test  ✅
```

**status=FAILED 원인 (정상 예상 결과):**
`.env`의 `SENDGRID_API_KEY=` 가 비어있어 SendGrid 401 반환.
발송 이력은 FAILED로 DB에 정상 저장됨.

실제 이메일 발송이 필요하면:
```
SENDGRID_API_KEY=SG.실제키값
SENDGRID_FROM_EMAIL=발신자이메일@도메인.com
```

### 최종 동작 상태 (재빌드 후)

| 기능 | 상태 | 비고 |
|------|------|------|
| 이메일로 사용자 조회 | ✅ | `findUser()` 정상 동작 |
| 알림 이력 DB 저장 | ✅ | PENDING → FAILED/SENT 기록 |
| 발송 이력 목록 조회 | ✅ | API 응답 정상 |
| SendGrid 실제 발송 | ❌ | API 키 미설정 (예상된 FAILED) |
| 어드민 페이지 새로고침 | ✅ | 라우터 가드 순서 수정 반영 |
| SMS phone 필드 | ✅ | 채널 SMS 선택 시 입력란 노출 |
| 발송 후 목록 갱신 | ✅ | 1.5초 후 invalidateQueries |

---

## [2026-04-17] Swagger UI 접근 불가 수정

### 메타 정보
- **날짜:** 2026-04-17
- **작업명:** `http://localhost/swagger-ui.html` 정상 작동 체크 및 수정
- **대상 파일:**
  - `frontend/nginx.conf`
  - `backend/src/main/java/com/example/app/config/SecurityConfig.java`

### 발견된 문제 2건

---

### 문제 1 (치명적) — nginx Swagger 경로 프록시 누락

**파일:** `frontend/nginx.conf`

**증상:** `http://localhost/swagger-ui.html` 접근 시 Vue 프론트엔드 `index.html` 반환

**원인:**
nginx `location /` 의 `try_files` 가 `/swagger-ui.html`, `/v3/api-docs` 등 Swagger 경로를
백엔드로 프록시하지 않고 Vue SPA의 `index.html`을 반환함.

```
브라우저 GET /swagger-ui.html
  → nginx: location / 매칭 → try_files → Vue index.html 반환
  → Swagger UI 대신 Vue 앱이 렌더링됨
```

**수정 내용:**

```nginx
# Swagger UI 프록시 (추가)
location /swagger-ui {
    proxy_pass http://backend:8080;
    ...
}

location /swagger-ui.html {
    proxy_pass http://backend:8080;
    ...
}

# OpenAPI 문서 프록시 (추가)
location /v3/api-docs {
    proxy_pass http://backend:8080;
    ...
}

# Swagger 웹자원 (추가)
location /webjars {
    proxy_pass http://backend:8080;
    ...
}
```

---

### 문제 2 (높음) — Spring Security Swagger 경로 차단

**파일:** `backend/.../config/SecurityConfig.java`

**증상:** nginx 프록시 적용 후에도 `/swagger-ui.html` 접근 시 `/login`으로 리다이렉트 (302)

**원인:**
`SecurityConfig`의 `permitAll()` 경로 설정 오류:
- `/swagger-ui.html` 미등록
- `/api-docs/**` 경로가 잘못됨 (실제 경로는 `/v3/api-docs/**`)

```java
// 수정 전 (잘못된 경로)
.requestMatchers("/swagger-ui/**", "/api-docs/**").permitAll()

// 수정 후
.requestMatchers("/swagger-ui/**", "/swagger-ui.html", "/v3/api-docs/**", "/v3/api-docs").permitAll()
```

---

### 수정 후 동작

| 엔드포인트 | 수정 전 | 수정 후 |
|-----------|---------|---------|
| `GET /swagger-ui.html` | 200 (Vue index.html) | 302 → `/swagger-ui/index.html` ✅ |
| `GET /swagger-ui/index.html` | 200 (Vue index.html) | 200 Swagger UI ✅ |
| `GET /v3/api-docs` | 200 (Vue index.html) | 200 OpenAPI JSON ✅ |

> `/swagger-ui.html` 302는 Spring Boot 내부 리다이렉트 (정상).
> 브라우저에서 최종 200으로 Swagger UI 페이지 렌더링 확인.

### 적용 방법
```bash
# nginx 이미지 재빌드 (nginx.conf 변경)
docker compose up --build frontend -d

# 백엔드 이미지 재빌드 (SecurityConfig.java 변경)
docker compose up --build backend -d
```
