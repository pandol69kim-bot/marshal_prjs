server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Vue Router SPA 폴백 (새로고침 시 404 방지)
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 백엔드 REST API 프록시
    location /api {
        proxy_pass http://backend:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 60s;
    }

    # OAuth2 인증 시작 엔드포인트만 프록시 (/oauth2/authorization/{provider})
    # /oauth2/callback 은 Vue 프론트엔드 라우트이므로 여기서 프록시하면 안 됨
    location /oauth2/authorization {
        proxy_pass http://backend:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # OAuth2 콜백 처리 (Spring Security) — /login/oauth2/** 만 프록시
    # /login 전체를 프록시하면 Vue Router의 /login 페이지가 Spring Security로 전달됨
    location /login/oauth2 {
        proxy_pass http://backend:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 정적 자산 캐싱 (JS/CSS/이미지)
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # 헬스체크
    location /health {
        return 200 "ok";
        add_header Content-Type text/plain;
    }
}
