# 프로젝트 작업 내역

## 메타 정보
- **날짜:** 2026-04-15
- **작업명:** 외부 시스템 연동 REST API 웹서비스 프로젝트 구성
- **기술 스택:** Vue 3 (Vite) + Spring Boot 3.3 (JDK 21)
- **저장 위치:** `output/project/`

---

## 세션 1 — 산출물 문서 작성 (2026-04-15 오전)

### 작업 내용
산출물 문서 12개 파일 생성 (기획 → 설계 → 개발 명세)

### 생성된 문서 파일

| 단계 | 파일 | 내용 |
|------|------|------|
| `01_idea_analyst/` | `idea_analysis_report.md` | 문제 정의, MVP 기능 우선순위, 기술 도전 과제 |
| `02_planner/` | `product_requirements.md` | PRD, User Story, API 명세 초안, 데이터 모델, 마일스톤 |
| `04_architect/` | `tech_stack.md` | Vue 3 + Spring Boot 3.3 기술 스택 선정 근거 |
| `04_architect/` | `architecture.md` | 시스템 구성도, JWT/OAuth2 인증 흐름, 외부 연동 패턴, 공통 응답 포맷 |
| `04_architect/` | `project_structure.md` | 도메인 기반 디렉토리 구조 (frontend/backend) |
| `05_frontend_dev/` | `frontend_setup.md` | Vite 설정, Axios proxy, TanStack Query, Tailwind |
| `05_frontend_dev/` | `frontend_composables.md` | Axios interceptor(401 자동 갱신), Pinia auth store, TanStack Query composable, Vue Router guards, OAuth2 callback |
| `05_frontend_dev/` | `frontend_components.md` | LoginPage (VeeValidate+Zod), ResourceListPage (필터/페이징), BasePagination, Toast |
| `06_backend_dev/` | `backend_setup.md` | pom.xml, application.yml, docker-compose.yml, .env.example |
| `06_backend_dev/` | `backend_routes.md` | ApiResponse<T> 공통 포맷, GlobalExceptionHandler, AuthController, ResourceController, SecurityConfig |
| `06_backend_dev/` | `backend_services.md` | Feign Client + Circuit Breaker(Resilience4j), WebClient 비동기 연동, 알림 서비스(@Async), S3 업로드, AOP 로깅 |
| `06_backend_dev/` | `backend_schema.md` | Flyway SQL 마이그레이션, JPA 엔티티 |
| (루트) | `산출물요약.md` | 전체 산출물 요약 및 아키텍처 다이어그램 |

---

## 세션 2 — 실제 프로젝트 파일 생성 (2026-04-15 오후)

### 작업 내용
`output/project/` 디렉토리에 프론트엔드 + 백엔드 실행 가능한 프로젝트 파일 89개 생성

---

### 프론트엔드 생성 파일 (`output/project/frontend/`)

#### 프로젝트 설정
| 파일 | 설명 |
|------|------|
| `package.json` | 의존성 정의 (vue, axios, @tanstack/vue-query, vee-validate, zod, tailwindcss 등) |
| `vite.config.ts` | Vite 설정 + `/api`, `/oauth2` 프록시 (→ localhost:8080) |
| `tsconfig.json` / `tsconfig.app.json` / `tsconfig.node.json` | TypeScript 설정 |
| `tailwind.config.js` | Tailwind CSS + primary 컬러 팔레트 |
| `postcss.config.js` | PostCSS (tailwindcss, autoprefixer) |
| `index.html` | HTML 진입점 |
| `.env.example` | 환경 변수 템플릿 |
| `.gitignore` | Git 제외 파일 목록 |

#### 소스 파일 (`src/`)
| 파일 | 설명 |
|------|------|
| `main.ts` | 앱 초기화 (Pinia, Router, VueQueryPlugin) |
| `App.vue` | 루트 컴포넌트 + ToastContainer |
| `assets/main.css` | Tailwind directives + 공통 유틸리티 클래스 |

#### 타입 정의 (`src/types/`)
| 파일 | 설명 |
|------|------|
| `api.types.ts` | `ApiResponse<T>`, `PaginatedResponse<T>`, `PageMeta`, `PageParams` |
| `auth.types.ts` | `LoginRequest`, `LoginResponse`, `TokenResponse`, `UserDto` |
| `resource.types.ts` | `ResourceDto`, `CreateResourceRequest`, `UpdateResourceRequest` |

#### API 레이어 (`src/api/`)
| 파일 | 설명 |
|------|------|
| `auth.api.ts` | login, refresh, logout, getMe |
| `resources.api.ts` | getList, getOne, create, update, delete |
| `notifications.api.ts` | getList, send |

#### HTTP 클라이언트 (`src/lib/`)
| 파일 | 설명 |
|------|------|
| `axios.ts` | Axios 인스턴스 + Request Interceptor(JWT 자동 첨부) + Response Interceptor(401 큐잉 방식 토큰 갱신) |
| `queryClient.ts` | TanStack Query QueryClient 설정 |

#### 상태 관리 (`src/stores/`)
| 파일 | 설명 |
|------|------|
| `auth.store.ts` | Pinia store — accessToken, user, isAuthenticated, isAdmin, login/logout/refreshToken/fetchMe |
| `notification.store.ts` | Toast 상태 관리 — addToast, removeToast |

#### Composables (`src/composables/`)
| 파일 | 설명 |
|------|------|
| `useResources.ts` | TanStack Query — useResourceList, useResource, useCreateResource, useUpdateResource, useDeleteResource |
| `useToast.ts` | showSuccess, showError, showWarning, showInfo |
| `usePagination.ts` | page, size, params, goToPage, nextPage, prevPage, resetPage |

#### 라우터 (`src/router/`)
| 파일 | 설명 |
|------|------|
| `index.ts` | Vue Router 설정 + Navigation Guard (requiresAuth, requiresGuest, requiresAdmin) |

#### 페이지 (`src/pages/`)
| 파일 | 설명 |
|------|------|
| `LoginPage.vue` | 이메일 로그인 + Google/Kakao 소셜 로그인 버튼 |
| `OAuth2CallbackPage.vue` | OAuth2 리다이렉트 처리 (token 쿼리 파라미터 수신) |
| `DashboardPage.vue` | 대시보드 (통계 카드 + 최근 리소스 목록) |
| `ResourceListPage.vue` | 리소스 목록 (필터, 테이블, 페이지네이션, 생성 모달) |
| `ResourceDetailPage.vue` | 리소스 상세 조회 + 삭제 |
| `admin/NotificationPage.vue` | 알림 발송 폼 + 발송 이력 테이블 (관리자 전용) |

#### 컴포넌트 (`src/components/`)
| 파일 | 설명 |
|------|------|
| `layouts/AppLayout.vue` | 사이드바 레이아웃 (네비게이션, 사용자 정보, 로그아웃) |
| `layouts/AuthLayout.vue` | 로그인 페이지 전용 레이아웃 |
| `ui/BaseButton.vue` | 재사용 버튼 (variant: primary/secondary/danger, size: sm/md/lg, loading) |
| `ui/BaseInput.vue` | 재사용 인풋 (label, error, v-model) |
| `ui/BasePagination.vue` | 페이지네이션 컴포넌트 (ellipsis 처리) |
| `ui/BaseToast.vue` | Toast 알림 컨테이너 (teleport, transition-group) |
| `features/auth/LoginForm.vue` | VeeValidate + Zod 로그인 폼 |
| `features/resources/ResourceFilter.vue` | 키워드/상태 필터 |
| `features/resources/ResourceForm.vue` | 리소스 생성 모달 폼 |

---

### 백엔드 생성 파일 (`output/project/backend/`)

#### 프로젝트 설정
| 파일 | 설명 |
|------|------|
| `pom.xml` | Maven 설정 (Spring Boot 3.3, JDK 21, Spring Cloud 2023.0.1) |
| `docker-compose.yml` | PostgreSQL 16, Redis 7, MinIO 컨테이너 |
| `.env.example` | 환경 변수 템플릿 (DB, JWT, OAuth2, S3, SendGrid, NCP SMS 등) |
| `.gitignore` | Git 제외 파일 목록 |
| `src/main/resources/application.yml` | 전체 애플리케이션 설정 |
| `src/main/resources/application-local.yml` | 로컬 개발 설정 (SQL 로그, DEBUG 레벨) |

#### DB 마이그레이션 (`src/main/resources/db/migration/`)
| 파일 | 설명 |
|------|------|
| `V1__init_schema.sql` | users, resources, external_api_logs, notification_histories 테이블 + 인덱스 + updated_at 트리거 |
| `V2__add_file_metadata.sql` | file_metadata 테이블 추가 |

#### 진입점
| 파일 | 설명 |
|------|------|
| `Application.java` | `@SpringBootApplication` + `@EnableFeignClients` + `@EnableAsync` |

#### 공통 (`common/`)
| 파일 | 설명 |
|------|------|
| `response/ApiResponse.java` | `success(data)`, `success(data, meta)`, `error(detail)` — 공통 응답 래퍼 |
| `exception/ErrorCode.java` | INVALID_CREDENTIALS, TOKEN_EXPIRED, RESOURCE_NOT_FOUND, EXTERNAL_API_ERROR 등 |
| `exception/BusinessException.java` | 비즈니스 로직 예외 |
| `exception/ExternalApiException.java` | 외부 API 연동 예외 |
| `exception/GlobalExceptionHandler.java` | `@RestControllerAdvice` — BusinessException, Validation, ExternalApi, 전역 예외 처리 |
| `aop/ExternalApiLoggingAspect.java` | `@Around` — 외부 API 클라이언트 호출 자동 로깅 (성공/실패 + 소요 시간) |

#### 설정 (`config/`)
| 파일 | 설명 |
|------|------|
| `SecurityConfig.java` | JWT Stateless, CORS, OAuth2 로그인, 권한 설정 |
| `CorsConfig.java` | CORS 설정 (allowed-origins 환경 변수 기반) |
| `FeignConfig.java` | Feign 로그 레벨, 공통 헤더 인터셉터, ErrorDecoder |
| `WebClientConfig.java` | WebClient 빈 (connect/read timeout, 요청/응답 로깅 필터) |
| `RedisConfig.java` | RedisTemplate<String, String> 빈 |
| `AsyncConfig.java` | `notificationExecutor` 스레드풀 (core=2, max=10, queue=100) |
| `SwaggerConfig.java` | SpringDoc OpenAPI 설정 + JWT Bearer 인증 스키마 |

#### Auth 도메인 (`domain/auth/`)
| 파일 | 설명 |
|------|------|
| `entity/User.java` | UUID PK, email, password, role(USER/ADMIN), provider(LOCAL/GOOGLE/KAKAO) |
| `repository/UserRepository.java` | findByEmail, findByProviderAndProviderId, existsByEmail |
| `dto/LoginRequest.java` | record + `@Email`, `@NotBlank` 검증 |
| `dto/LoginResponse.java` / `TokenResponse.java` / `UserDto.java` | 응답 DTO (record) |
| `service/JwtProvider.java` | Access Token(15분), Refresh Token(7일) 발급/검증 |
| `service/AuthService.java` | 로그인, 토큰 갱신(Redis 블랙리스트), 로그아웃, HttpOnly Cookie 처리 |
| `service/OAuth2UserService.java` | Google/Kakao OAuth2 사용자 정보 처리 + 자동 회원가입 |
| `filter/JwtAuthenticationFilter.java` | `OncePerRequestFilter` — Bearer 토큰 검증 + SecurityContext 설정 |
| `handler/OAuth2SuccessHandler.java` | OAuth2 성공 후 JWT 발급 → 프론트엔드 redirect |
| `controller/AuthController.java` | POST /api/v1/auth/login, /refresh, /logout |

#### Resource 도메인 (`domain/resource/`)
| 파일 | 설명 |
|------|------|
| `entity/Resource.java` | BIGSERIAL PK, name, description, status(ACTIVE/INACTIVE/DELETED), owner(User) |
| `repository/ResourceRepository.java` | JPQL 동적 필터 쿼리 (status, keyword) |
| `dto/ResourceDto.java` / `CreateResourceRequest.java` / `UpdateResourceRequest.java` | 요청/응답 DTO (record) |
| `service/ResourceService.java` | findAll(필터+페이징), findById, create, update, delete(소프트) |
| `controller/ResourceController.java` | GET/POST/PUT/DELETE /api/v1/resources, `@PreAuthorize` 삭제 권한 제한 |

#### Notification 도메인 (`domain/notification/`)
| 파일 | 설명 |
|------|------|
| `entity/NotificationHistory.java` | 발송 이력 엔티티 (channel, status: PENDING/SENT/FAILED) |
| `repository/NotificationHistoryRepository.java` | findByUserId 페이징 |
| `adapter/EmailAdapter.java` / `SmsAdapter.java` | 어댑터 인터페이스 |
| `service/NotificationService.java` | `@Async("notificationExecutor")` — 비동기 이메일/SMS 발송 + 이력 저장 |

#### Infrastructure (`infrastructure/`)
| 파일 | 설명 |
|------|------|
| `external/client/ExternalDataClient.java` | `@FeignClient` 선언적 외부 연동 + Fallback 연결 |
| `external/client/ExternalDataResponse.java` | 외부 API 응답 record |
| `external/client/SendGridClient.java` | `@FeignClient` — SendGrid 이메일 발송 |
| `external/fallback/ExternalDataClientFallback.java` | Circuit Breaker Fallback — 빈 응답 반환 |
| `external/AsyncExternalService.java` | WebClient 비동기 외부 API 호출 (retry/timeout/에러 처리) |
| `storage/S3StorageService.java` | S3 직접 업로드 + Presigned URL 발급 (MinIO 호환) |

#### 테스트 (`src/test/`)
| 파일 | 설명 |
|------|------|
| `domain/auth/AuthControllerTest.java` | `@WebMvcTest` — 로그인 성공/유효성 오류 |
| `domain/resource/ResourceServiceTest.java` | `@ExtendWith(MockitoExtension)` — 서비스 단위 테스트 |
| `infrastructure/ExternalClientTest.java` | WireMock — 외부 API 통합 테스트 |

---

## 아키텍처 요약

```
[Vue 3 SPA : 5173]
    │ HTTPS + JWT Bearer
    ▼
[Spring Boot 3.3 : 8080]
    ├── Spring Security (JWT Filter + OAuth2)
    ├── REST Controllers (ApiResponse<T>)
    ├── Service Layer (@Transactional)
    ├── Repository (Spring Data JPA)
    │
    ├── [PostgreSQL 16 : 5432]  ← Flyway 마이그레이션
    ├── [Redis 7 : 6379]        ← 토큰 블랙리스트 / 캐시
    ├── [MinIO : 9000]          ← S3 호환 파일 스토리지
    │
    └── External Systems
        ├── Google / Kakao OAuth2
        ├── SendGrid (Email)
        ├── NCP SMS
        └── 외부 REST API (Feign + WebClient + Resilience4j)
```

---

## 개발 착수 순서 (권장)

| Phase | 기간 | 주요 작업 |
|-------|------|---------|
| Phase 1 | 2주 | Spring Boot 초기 설정, Flyway 스키마, JWT 인증, Vue + Axios interceptor |
| Phase 2 | 2주 | CRUD REST API + 페이지네이션, Vue 페이지/컴포넌트, TanStack Query |
| Phase 3 | 2주 | OAuth2 소셜 로그인, Feign Client 외부 연동, 알림 서비스 |
| Phase 4 | 1주 | Circuit Breaker, 테스트 커버리지 80%+, Swagger 문서화 |

---

## 빠른 실행

```bash
# 1. 인프라 기동
cd output/project/backend
docker-compose up -d

# 2. 백엔드 실행
cp .env.example .env   # 필수 값 입력
./mvnw spring-boot:run -Dspring-boot.run.profiles=local

# 3. 프론트엔드 실행
cd output/project/frontend
cp .env.example .env
npm install
npm run dev
```

| 서비스 | URL |
|--------|-----|
| 프론트엔드 | http://localhost:5173 |
| 백엔드 API | http://localhost:8080 |
| Swagger UI | http://localhost:8080/swagger-ui.html |
| MinIO Console | http://localhost:9001 |
